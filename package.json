{
  "name": "composing-software",
  "version": "0.0.0",
  "private": true,
  "description": "Code examples + playground based on the book by Eric Elliott",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "check": "npm run typecheck && npm run fmt:check"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "oxfmt": "^0.56.0",
    "typescript": "^5.0.0"
  }
}
